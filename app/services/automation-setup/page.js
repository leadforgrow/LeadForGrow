import MarketingLayout from '@/app/components/MarketingLayout';

export default function AutomationSetupPage() {
  return (
    <MarketingLayout 
      title="Automation Setup" 
      subtitle="Expertly crafted automation workflows that handle nurturing, reminders, and notifications so you never miss a beat."
    />
  );
}
